import React, { useState, useRef, useEffect } from 'react';
import {shallow} from 'enzyme';

import ListItem from '@material-ui/core/ListItem';
import ListItemText from '@material-ui/core/ListItemText';
import SingleMsg from'./SingleMsg';
/** 
describe('Chat', () => {
        let result;
        
        const index=1;
        beforeEach(() => {
          result = shallow(<SingleMsg webId="https://uo225211.solidcommunity.net/profile/card#me"
          d={new Date()} index={index}/>);
        });
  
        it('SingleMsg', () => {
            const item = result.find(ListItem);
            expect(item).toBeTruthy();
        });
        it('SingleMsg', () => {
          const singe = result.find(ListItemText);
          expect(singe).toBeTruthy();
      });
      
      it('shows an empty list', () => {
        const scrooll = result.find(InfiniteScroll);
        expect(scrooll).toBeTruthy();
      });
    });
*/

